import React from 'react';
import Hero from './Hero';  // Importing the Hero section

const Homepage = () => {
    return (
        <div>
            <Hero />
        </div>
    );
};

export default Homepage;
