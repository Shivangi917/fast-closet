# Project Setup

## Start Client

Run the following command to start the client:

```bash
npm run dev
```

## Start Server

Run the following command to start the server:

```bash
nodemon index.js
```
